# Packages the guide treats as "always loaded", and which therefore do NOT
# trigger a warning about a missing library() call in every single code block.
#
# The reasoning: the tidyverse core is introduced in Phase 2 and assumed from
# there on. A lookup or cheat-sheet snippet using mutate() or select() should
# not be forced to repeat library(dplyr) every time.
#
# EVERYTHING ELSE must be loaded explicitly where it is used. Those are the
# specialist packages (fastreg, tableone, gtsummary, mice, arrow ...) that a
# reader has not already loaded, and where a missing library() gives
# "could not find function" on the first run.

dplyr
tibble
tidyr
ggplot2
stringr
purrr
readr
forcats
lubridate
magrittr
tidyverse
